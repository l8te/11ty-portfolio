---
layout: base.njk
---
<p>this uses the "page" template</p>
{{ content | safe }}

<p>this should have EITHER a list of books OR a list of shows</p>

<ul>
    {% for group in collections.group %}
    {% if group.data.eleventyNavigation.parent == eleventyNavigation.key %}
    <li><a href="{{ group.url }}"><img src="/img/{{ group.data.image }}" alt="{{ description }}" style="max-height: 100px;" />{{ group.data.title }}</a></li>
    {% endif %}
    {% endfor %}
</ul>