<div class="center ph0-ns">
    <ul class="list pl0 cf ph0-ns">
        {% for post in postslist | reverse %}
        <li class="fl w-100 w-25-ns pr3 pb3 mb3">
            <div class="bt bw3">
                <h3 class="pt3 pb3 ma0">
            <a href="{{ post.url | url }}" class="link dib underline hover-bg-hot-pink">
                {{ post.data.title }}
            </a>
            </h3>
        </div>
            <a href="{{ post.url }}" class="link dim"><img src="/img/{{ post.data.image }}" alt="{{ description }}" class="img dim" /></a>
            <ul class="list pl0">
                {% for tag in post.data.tags %}
                {% if tag == "group" %}
                {% elif tag == "item" %}
                {% elif tag == "featured" %}
                {% else %}
                <li class="fl pr3">
                    {% set tagUrl %}/tags/{{ tag }}/{% endset %}
                    <a href="{{ tagUrl | url }}" class="link underline hover-bg-hot-pink">{{ tag }}</a>
                </li>
                {% endif %}
                {% endfor %}
            </ul>
            {% endfor %}
        </li>
    </ul>
</div>
