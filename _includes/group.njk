---
layout: base.njk
---

<p>this uses the "groups" template</p>
{{ content | safe }}

<!-- there is probably a better way to do this -->
<!-- ID comparason isn't working? -->

<ul>
    {% for group in collections.group %}

    <li><a href="{{ group.url }}"> <img src="../groups/img/{{ group.data.image }}" style="max-height: 100px;" /> - {{ group.data.title }} - {{ group.data.description }}</a></li>

    {% endfor %}
</ul>
