---
layout: base.njk
---

<p>this uses the "group" template</p>
{{ content | safe }}

<p>this should have a list of items</p>

<ul>
    {% for item in collections.item %}
    {% if item.data.ID == ID %}
    <li><a href="{{ item.url }}">
<img src="/img/{{ item.data.image }}" alt="{{ description }}" style="max-height: 100px;" />{{ item.data.title }}</a></li>
    {% endif %}
    {% endfor %}
</ul>