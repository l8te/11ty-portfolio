<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>{{ metadata.title }}</title>
    <meta name="description" content="">
    <meta name="author" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/tachyons.css">
    <!-- <link rel="stylesheet" href="/css/custom.css"> -->
</head>

<body class="bg-near-white mid-gray">
    {{ collections.nav | eleventyNavigation | eleventyNavigationToHtml | safe }}
    <h1>{{ title }}</h1>
    <h2>{{ description }}</h2>
    {{ content | safe }}
    <!-- here: eval if this is home, if home show "shows" list -->
    <ul>
        {%- for show in collections.show -%}
        {%- if page.url == "/" -%}
        <li><a href="{{ show.url }}"> {{ show.data.title }} - {{ show.data.description }}</a></li>
        {%- endif -%}
        {%- endfor -%}
    </ul>
</body>
<footer>
    &copy; {{ metadata.title }} | this is some footer
    <br />
    <!-- this is for QA - shows all pages -->
    <hr />
    <ul>
        {%- for post in collections.all -%}
        <li><a href="{{ post.url }}">{{ post.url }}</a></li>
        {%- endfor -%}
    </ul>
</footer>

</html>